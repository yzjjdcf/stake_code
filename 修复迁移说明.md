# 修复数据库迁移问题

## 问题
数据库中存在重复的 `code` 记录，导致添加唯一约束失败。

## 解决方案

### 方法1：回滚并重新应用（推荐）

```bash
cd stake

# 1. 回滚到迁移 0006
python manage.py migrate serverbot 0006

# 2. 重新应用迁移（现在会先清理重复数据）
python manage.py migrate
```

### 方法2：手动清理重复数据（如果方法1不行）

如果回滚失败，可以手动清理重复数据：

```bash
cd stake
python manage.py shell
```

然后在 Django shell 中执行：

```python
from serverbot.models import CodeRecord, ClaimRecord
from django.db.models import Count

# 查找重复的 code
duplicates = CodeRecord.objects.values('code').annotate(
    count=Count('id')
).filter(count__gt=1)

# 清理重复记录
for dup in duplicates:
    code = dup['code']
    records = CodeRecord.objects.filter(code=code).order_by('created_at')
    
    if records.count() > 1:
        first_record = records.first()
        
        # 将其他记录的 ClaimRecord 关联到第一个记录
        for record in records[1:]:
            ClaimRecord.objects.filter(code_record=record).update(
                code_record=first_record
            )
            record.delete()
        
        print(f"清理重复代码: {code}")

# 退出
exit()
```

然后运行迁移：

```bash
python manage.py migrate
```

### 方法3：直接修改数据库（最后手段）

如果以上方法都不行，可以临时移除唯一约束，清理数据后再添加：

```bash
cd stake
python manage.py dbshell
```

在 SQLite shell 中：

```sql
-- 查看重复数据
SELECT code, COUNT(*) as count 
FROM serverbot_coderecord 
GROUP BY code 
HAVING count > 1;

-- 手动删除重复记录（保留最早的）
-- 注意：需要根据实际情况调整
```

然后退出并运行迁移。

## 验证

迁移成功后，可以验证：

```bash
python manage.py shell
```

```python
from serverbot.models import CodeRecord
# 检查是否还有重复
from django.db.models import Count
duplicates = CodeRecord.objects.values('code').annotate(
    count=Count('id')
).filter(count__gt=1)
print(f"重复记录数: {duplicates.count()}")
# 应该输出 0
```

